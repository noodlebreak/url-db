#!/usr/bin/env bash
# Legacy pico.sh deploy — no longer used (pgs is pico+ only).
# Site is deployed via GitHub Pages: push to master.
echo "Deploy: git push origin master → https://noodlebreak.github.io/url-db/"
